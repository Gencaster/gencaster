services:
  editor-local:
    build:
      context: caster-editor
      dockerfile: Dockerfile
      # args:
      #   - BACKEND_GRAPHQL_URL=https://127.0.0.1:8081/graphql
    ports:
      - 3001:3001
    volumes:
      - ./caster-editor/src/:/root/caster-editor/src/
    # environment:
      # - NGINX_HOST=0.0.0.0
    depends_on:
      - backend
