overwrite: true
schema: '../../caster-back/schema.gql'
documents: '../../caster-back/*.gql'

generates:
  src/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-vue-urql
